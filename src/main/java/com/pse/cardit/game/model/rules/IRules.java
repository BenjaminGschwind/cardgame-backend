package com.pse.cardit.game.model.rules;

/**
 * IRules is an interface that provides the possibility to group several concrete classes implementing
 * this interface in one collection.
 */
public interface IRules {
}
